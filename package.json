{
  "name": "devcon-2017-process",
  "description": "Think about process",
  "author": "robertwhiting",
  "registry": "npm",
  "scripts": {
    "start": "reveal-md -w --port 8888 slides.md",
    "build": "reveal-md --static dist/ slides.md && cp -r ./images dist/ && reveal-md --print dist/$npm_package_name.pdf --theme $npm_package_theme slides.md"
  },
  "devDependencies": {
    "reveal-md": "0.4.2"
  }
}
